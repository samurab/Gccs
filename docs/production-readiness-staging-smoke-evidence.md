# Production Readiness Staging Smoke Evidence

Story: PR-3.1 - Deploy And Smoke Test Staging.

Evidence status: Blocked - live staging exists, but approved CI/CD deployment evidence and required `/health` dependency signals are not attached.

Evidence date: 2026-07-01.

Evidence owner: Engineering lead.

Approved deployment path: `.github/workflows/staging.yml`.

Expected environment: GitHub Actions `staging` environment.

Expected artifact source: API artifact published from `apps/api/Gccs.Api.csproj` in Release configuration and web artifact built from `apps/web`.

Expected migration source: EF Core idempotent migration script generated by the staging workflow.

## Attempted Local Verification

| Check | Result | Evidence |
| --- | --- | --- |
| Staging workflow exists | Passed | `.github/workflows/staging.yml` defines `workflow_dispatch`, `main`, and `release/**` triggers. |
| Approved workflow builds API and web artifacts | Passed | Workflow runs `dotnet publish apps/api/Gccs.Api.csproj --configuration Release` and `npm run build:web`. |
| Approved workflow generates idempotent EF migration script | Passed | Workflow runs `dotnet tool run dotnet-ef migrations script --idempotent`. |
| Approved workflow validates No-CUI and no-production-data guardrails | Passed | Workflow checks `STAGING_CUSTOMER_DATA_MODE=synthetic-only`, `No-CUI`, and `no production customer data`. |
| Approved workflow calls staging `/health` | Passed | Workflow calls `curl "$STAGING_API_BASE_URL/health"` and uploads `staging-smoke-test-results`. |
| Local environment has staging URL and credentials | Failed | No `STAGING_API_BASE_URL`, `STAGING_WEB_BASE_URL`, cloud identity, database, storage, cache, queue, or secret-store credentials were available in the local execution environment. |
| Live staging deployment executed | Blocked | Requires GitHub staging environment variables/secrets and workflow execution. |
| Live `/health` smoke output attached | Blocked | Requires deployed staging API and `staging-health.json` artifact. |

## Live Resource Group Smoke Check - 2026-07-01

Resource group: `gccs-staging-rg`.

| Check | Result | Evidence |
| --- | --- | --- |
| Staging resource group exists | Passed | Azure resource group `gccs-staging-rg` contains API, App Service plan, PostgreSQL, Static Web App, and storage resources. |
| API App Service exists | Passed | `gccs-api-staging-19984` in East US. |
| Frontend Static Web App exists | Passed | `gccs-web-staging-19984` in East US 2, default hostname `mango-rock-016ff040f.7.azurestaticapps.net`. |
| PostgreSQL database exists | Passed | PostgreSQL flexible server `gccs-pg-staging-19984` includes database `gccs`. |
| Storage account and containers exist | Passed | Storage account `gccsstaging19984` contains private containers `evidence`, `exports`, and `reports`. |
| Storage upload containers contain no existing blobs | Passed | Blob counts on 2026-07-01: `evidence = 0`, `exports = 0`, `reports = 0`. |
| API storage managed identity access | Passed | API managed identity has `Storage Blob Data Contributor` on `gccsstaging19984`. |
| Staging API `/health` reachable | Partial | `GET https://gccs-api-staging-19984.azurewebsites.net/health` returned HTTP 200 with `service = gccs-api` and `dataPosture = No-CUI / compliance management only`. |
| Staging API `/health` dependency signals | Failed | Current deployed API returned `dependencies = []`; required `postgresql`, `redis`, `object-storage`, and `background-jobs` signals were not present. Local code has been updated to emit required runtime dependency signals, but the updated API has not yet been deployed through the approved CI/CD path. |
| Redis/cache and background job dependency | Blocked | No Redis/cache resource is present in `gccs-staging-rg`; PR-3.1 requires `redis` and `background-jobs` health signals before closure. |
| Production secret exposure | Mitigated | During verification, a database connection string was observed through App Service settings output. The PostgreSQL admin password was rotated and the API App Service connection string was updated on 2026-07-01. No secret value is recorded in this evidence file. |

## Required `/health` Signals

The staging smoke test must attach a `staging-health.json` artifact showing:

- `service = gccs-api`
- `status = ok`
- `dataPosture = No-CUI / compliance management only`
- dependency `postgresql`
- dependency `redis`
- dependency `object-storage`
- dependency `background-jobs`

The API `/health` endpoint currently emits `service`, `status`, `dataPosture`, `checkedAt`, and dependency signals from `LocalDependencyHealthService`.

## Data Guardrail Evidence Required

Before PR-3.1 can be closed for launch approval, staging evidence must confirm:

- No production customer data.
- No real customer CUI.
- No production secrets.
- No production uploads.
- No production unrestricted logs.
- Synthetic-only staging data.
- No-CUI / compliance management only posture.

## Launch Blocker

| Blocker ID | Blocker | Owner | Severity | Mitigation | Contingency | Target date | Current status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| STAGE-GAP-001 | Live staging deployment and `/health` smoke evidence are not attached. | Engineering lead | High | Run `.github/workflows/staging.yml` in the GitHub `staging` environment with staging variables/secrets configured, then attach `staging-smoke-test-results`. | If staging cannot be executed, keep production launch blocked and do not proceed to production deployment stories. | Before PR-6.1 launch approvals | Open |
| STAGE-GAP-002 | Current deployed staging `/health` output does not include required dependency signals. | Engineering lead | High | Deploy the updated API health implementation through the approved staging CI/CD workflow and attach `staging-health.json`. | Keep PR-3.1 blocked if `dependencies` does not include `postgresql`, `redis`, `object-storage`, and `background-jobs`. | Before PR-3.1 closure | Open |
| STAGE-GAP-003 | Staging has no Redis/cache or background job queue resource. | Engineering lead | High | Provision the approved staging Redis/cache and background job queue dependency, configure the API, and rerun `/health`. | Keep Redis-dependent background job workflows deferred and keep PR-3.1 blocked. | Before PR-3.1 closure | Open |

## PR-3.1 Disposition

PR-3.1 cannot be marked launch-complete until the updated API is deployed through the approved CI/CD path, live staging `/health` emits required dependency signals, Redis/background job dependencies are configured, and smoke output is attached to the launch package.
